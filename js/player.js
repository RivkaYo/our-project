const chooseMeme = () => {
    //import memes from photos.js
    let chosenMeme = memes[Math.floor(Math.random * 9)];
    return chosenMeme;
};

//need to set img crs at player.html to chooseMeme


//save player1's answer (value) to a vareable.

